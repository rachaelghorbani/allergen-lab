<h1><%= ("#{@user.name.titlecase}'s").pluralize %> Recipes</h1>
<ul>
<% @user.recipes.each do |recipe| %>
    <li><%= link_to recipe.name, recipe_path(recipe) %></li>
<% end %>
</ul>

<%= button_to "delete", user_path(@user), method: :delete %>
